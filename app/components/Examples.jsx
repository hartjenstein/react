let React = require('react');
let Nav = require('Nav');

let Examples = (props) => {
    return (
      <div>
        <h2>Examples</h2>
        <p>Welcome to the example page</p>
      </div>
    );
};

module.exports = Examples;